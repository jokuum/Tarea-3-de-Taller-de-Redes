# ── macOS ──────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Docker ─────────────────────────────
# Datos persistidos por volúmenes (nunca se deben commitear)
**/volumes/
**/data/

# Archivos de override locales de Compose
docker-compose.override.yml
docker-compose.local.yml

# ── Editores e IDEs ─────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Logs ───────────────────────────────
*.log
logs/

# ── Variables de entorno sensibles ─────
.env
.env.*
!.env.example
